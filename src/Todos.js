import TaskSubmit from "./TaskSubmit";

const Todos = () => {

    return (
      <div className="body-container">
        <header>todos</header>

        <TaskSubmit />


      </div>
    );
}

export default Todos;